# Retained primary evidence

`prospective/` contains the unchanged primary aggregate analysis and manifests.
`data/` contains the original HBN source manifests (participant IDs, ages and
splits) and their existing metadata. The 800/100 split identity matches the
primary study. Historical screening/fine-tuning results remain in the original
research archive. The paper's three extensions are under `../extensions/`.
