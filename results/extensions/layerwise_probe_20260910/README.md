# Layer-wise probe analysis

This directory contains aggregate-only outputs from the secondary exploratory
layer-wise REVE age-probing analysis. It contains no raw EEG, participant-level
predictions, or participant identifiers.

The report is bound to the immutable remote external-evaluation lock and
prediction inventory by SHA-256. The PDF and LaTeX files are generated from
the aggregate report and can be reviewed before manuscript integration.
